overwrite: true
schema: "generated/schema.graphql"
documents: "pages/**/*.tsx"
generates:
  generated/codegen.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
