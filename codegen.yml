overwrite: true
schema: "generated/schema.graphql"
documents: "pages/**/*.tsx"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
